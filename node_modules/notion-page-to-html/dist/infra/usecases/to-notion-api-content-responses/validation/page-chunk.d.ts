import { Validation } from '../../../protocols/validation';
export declare class PageChunkValidator implements Validation {
    private readonly _notionPageId;
    private readonly _pageChunkStatus;
    constructor(notionPageId: string, pageChunkStatus: number);
    validate(): Error | null;
}
